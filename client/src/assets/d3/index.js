import { select, selectAll } from 'd3-selection';

import {
  scaleLinear,
  scaleTime,
  scaleOrdinal,
} from 'd3-scale';

import { axisTop } from 'd3-axis';

import {
  geoMercator,
  geoPath
} from 'd3-geo';

import {
  json
} from 'd3-fetch';

export default {
  select,
  selectAll,
  scaleLinear,
  scaleTime,
  scaleOrdinal,
  axisTop,
  geoMercator,
  geoPath,
  json
};